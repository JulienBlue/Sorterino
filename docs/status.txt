📦 Projektübergabe – Sorterino v0.2.9 → Start v0.3.0 (MailDrop)
🔹 Aktueller Stand: v0.2.9 – Clean Baseline

Sorterino befindet sich auf einem stabilen, bereinigten Architekturstand.

✅ Architektur

Clean Architecture (Domain / Usecases / Infrastructure / Interfaces)

Rein dateibasierte Verarbeitung

Eine DocumentSource: FolderDocumentSource

Keine Mail-Integration im Code

Keine Polling-Logik

Keine toten Artefakte

Runtime-Isolation via .sorterino_runtime

Tagesbasiertes Logging

Backup vor Verarbeitung

Duplikatschutz bei Speicherung

Pipeline läuft stabil.

🔹 Aktueller Dokumentenfluss
User legt Datei ab →
.sorterino_runtime/incoming →
Pipeline →
OCR →
Klassifikation →
Rename →
Archivierung

Mail ist nicht technisch integriert.

🎯 Ziel von v0.3.0

Einführung eines MailDrop-Workflows über Outlook-Regeln
ohne Änderung der Kernarchitektur.

Wichtig:
Keine direkte E-Mail-API-Integration.
Kein IMAP-Zugriff.
Keine MailReader-Komponente.

Stattdessen:

Outlook speichert Anhänge automatisch im Sorterino-Input-Ordner.

📌 Geplante Umsetzung v0.3.0
Phase 1 – Technisch (keine Codeänderung)

Outlook-Regel speichert Anhänge nach:

.sorterino_runtime/incoming

Pipeline verarbeitet diese wie normale Dateien.

Kein Code-Refactor notwendig.

Phase 2 – Dokumentation & Nutzeranleitung

Erweiterung der Anwenderdokumentation um:

📧 Einrichtung der Outlook-Regel

Regel erstellen

Bedingung „mit Anlagen“

Aktion „Anlage speichern“

Zielordner: Sorterino-Input

🔐 Benutzerhinweise

Nur unterstützte Formate

Keine ZIP-Dateien

Keine mehrfachen Weiterleitungen

Optional: nur bestimmte Absender

🧠 Architekturelle Entscheidung

Mail wird bewusst:

Nicht als eigene Infrastruktur-Komponente integriert

Nicht als neue DocumentSource implementiert

Sondern als externe Systemregel behandelt

Begründung:

Geringere Komplexität

Höhere Stabilität

Kein zusätzlicher Wartungsaufwand

Saubere MVP-Strategie

IHK-konforme Systemabgrenzung

📘 Ziel im neuen Chat

Im neuen Chat sollen wir:

Outlook MailDrop sauber definieren

Eine klare Schritt-für-Schritt-Anleitung für Nutzer formulieren

README oder Anwenderdokumentation erweitern

Optional: Screenshots strukturieren (Platzhalter)

Text IHK-tauglich formulieren

🔎 Offene Designfrage für v0.3.0

Im neuen Chat klären wir:

Möchtest du:

A) Nur Mail → Input
B) Mail → separater Unterordner in incoming
C) Mail-Metadaten später berücksichtigen

Aktuell tendierst du zu:
A – Minimaler MVP MailDrop

🚀 Startpunkt für neuen Chat

Im neuen Chat bitte:

„Wir implementieren v0.3.0 – MailDrop über Outlook-Regel. Ich möchte die Nutzeranleitung formulieren.“

Dann bauen wir:

technische Anleitung

Anwenderleitfaden

saubere Dokumentationserweiterung

IHK-konforme Beschreibung